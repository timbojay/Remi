# Remi — Environment Configuration

# Your name (used in biography prompts)
USER_NAME=Tim

# Ollama settings (local LLM — no API key needed)
OLLAMA_BASE_URL=http://localhost:11434
MODEL_NAME=qwen3:8b

# Database path (defaults to backend/data/remi.db)
# DB_PATH=/path/to/custom/remi.db
